import styled from "styled-components";
import img from "./../../assets/img/bg_login.jpg";

const StyledViewLogin = styled.div`
  background: #0b0f27 url(${img}) no-repeat 0 0;
  background-size: cover;
`;

export default StyledViewLogin;
